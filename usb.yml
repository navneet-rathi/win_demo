---
- name: Disable USB storage and enable one USB port on Windows 7
  hosts: all
  gather_facts: false
  tasks:
    
    # Task 1: Disable USB storage via Registry
    - name: Disable USB storage by modifying registry
      ansible.windows.win_regedit:
        path: 'HKLM:\SYSTEM\CurrentControlSet\Services\USBSTOR'
        name: 'Start'
        data: 4
        type: dword
     # become: yes

   # - name: Reboot system after whitelisting USB device (if required)
   #   win_reboot:
   #     reboot_timeout: 120     
    

- name: Enable a specific USB device by PnP ID
  hosts: all
  gather_facts: false
  #become: yes
  tasks:

    # Retrieve USB device information (PnP ID)
    - name: Get list of USB devices and their IDs
      ansible.windows.win_shell: |
        Get-WmiObject Win32_PnPEntity | Where-Object { $_.Description -like "*USB*" } | Select-Object DeviceID, Description
      register: usb_devices

    - name: Display USB devices
      debug:
        var: usb_devices.stdout_lines

    # Enable the specific USB device
    - name: Enable the USB device using PnP ID
      ansible.windows.win_shell: |
        $device = Get-WmiObject Win32_PnPEntity | Where-Object { $_.DeviceID -eq 'USB\VID_0627&PID_0001\89126-0000:00:1D.7-2' }
        if ($device) {
          pnputil /enable-device USB\VID_0627&PID_0001\89126-0000:00:1D.7-2
        }
      #when: usb_devices.stdout_lines is search('USB\VID_0627&PID_0001\89126-0000:00:1D.7-2')  # Replace with your actual PnP ID   